/*
 * This file is autogenerated by libhelfem/src/generate_hip_code.py.
 *
 *                This source code is part of
 *                          HelFEM
 *
 * Written by Susi Lehtola, 2018-
 * Copyright (c) 2018- Susi Lehtola
 *
 * SPDX-License-Identifier: BSD-3-Clause
 */
#ifndef LIB1DFEM_HIPBASIS_EVAL_H
#define LIB1DFEM_HIPBASIS_EVAL_H

#include <armadillo>
#include <lib1dfem/LIPBasis_eval.h>
#include <sstream>
#include <stdexcept>

namespace helfem {
namespace lib1dfem {
namespace polynomial_basis {
namespace detail {

/// Evaluate the n-th derivative of every HIP basis function on the
/// reference element [-1, 1] at the given points x, given control nodes
/// x0 and the precomputed LIP-derivative-at-nodes vector lipxi. Fills
/// dnf (size x.n_elem x 2*x0.n_elem) with paired function-and-derivative
/// shape functions:
///   - column 2*fi   : the value-interpolating Hermite function for node fi
///   - column 2*fi+1 : the derivative-interpolating Hermite function for node fi
/// scaled by element_length on the derivative slot.
template <typename T>
void eval_hip_prim_dnf(const arma::Col<T> & x, const arma::Col<T> & x0,
                       const arma::Col<T> & lipxi,
                       arma::Mat<T> & dnf, int n, T element_length) {
  switch (n) {
case (0): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T f2 = flip(ix, fi) * flip(ix, fi);
      dnf(ix, 2*fi)     = f1 * f2;
      dnf(ix, 2*fi + 1) = f3 * f2 * element_length;
    }
  }
} break;
case (1): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T f2 = flip(ix, fi) * flip(ix, fi);
      T df2 = T(2) * dflip(ix, fi) * flip(ix, fi);
      dnf(ix, 2*fi)     = T(1) * df1 * f2 + f1 * df2;
      dnf(ix, 2*fi + 1) = (T(1) * df3 * f2 + f3 * df2) * element_length;
    }
  }
} break;
case (2): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T df2 = T(2) * dflip(ix, fi) * flip(ix, fi);
      T d2f2 = T(2) * d2flip(ix, fi) * flip(ix, fi) + T(2) * dflip(ix, fi) * dflip(ix, fi);
      dnf(ix, 2*fi)     = T(2) * df1 * df2 + f1 * d2f2;
      dnf(ix, 2*fi + 1) = (T(2) * df3 * df2 + f3 * d2f2) * element_length;
    }
  }
} break;
case (3): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d2f2 = T(2) * d2flip(ix, fi) * flip(ix, fi) + T(2) * dflip(ix, fi) * dflip(ix, fi);
      T d3f2 = T(2) * d3flip(ix, fi) * flip(ix, fi) + T(6) * d2flip(ix, fi) * dflip(ix, fi);
      dnf(ix, 2*fi)     = T(3) * df1 * d2f2 + f1 * d3f2;
      dnf(ix, 2*fi + 1) = (T(3) * df3 * d2f2 + f3 * d3f2) * element_length;
    }
  }
} break;
case (4): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d3f2 = T(2) * d3flip(ix, fi) * flip(ix, fi) + T(6) * d2flip(ix, fi) * dflip(ix, fi);
      T d4f2 = T(2) * d4flip(ix, fi) * flip(ix, fi) + T(8) * d3flip(ix, fi) * dflip(ix, fi) + T(6) * d2flip(ix, fi) * d2flip(ix, fi);
      dnf(ix, 2*fi)     = T(4) * df1 * d3f2 + f1 * d4f2;
      dnf(ix, 2*fi + 1) = (T(4) * df3 * d3f2 + f3 * d4f2) * element_length;
    }
  }
} break;
case (5): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d4f2 = T(2) * d4flip(ix, fi) * flip(ix, fi) + T(8) * d3flip(ix, fi) * dflip(ix, fi) + T(6) * d2flip(ix, fi) * d2flip(ix, fi);
      T d5f2 = T(2) * d5flip(ix, fi) * flip(ix, fi) + T(10) * d4flip(ix, fi) * dflip(ix, fi) + T(20) * d3flip(ix, fi) * d2flip(ix, fi);
      dnf(ix, 2*fi)     = T(5) * df1 * d4f2 + f1 * d5f2;
      dnf(ix, 2*fi + 1) = (T(5) * df3 * d4f2 + f3 * d5f2) * element_length;
    }
  }
} break;
case (6): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d5f2 = T(2) * d5flip(ix, fi) * flip(ix, fi) + T(10) * d4flip(ix, fi) * dflip(ix, fi) + T(20) * d3flip(ix, fi) * d2flip(ix, fi);
      T d6f2 = T(2) * d6flip(ix, fi) * flip(ix, fi) + T(12) * d5flip(ix, fi) * dflip(ix, fi) + T(30) * d4flip(ix, fi) * d2flip(ix, fi) + T(20) * d3flip(ix, fi) * d3flip(ix, fi);
      dnf(ix, 2*fi)     = T(6) * df1 * d5f2 + f1 * d6f2;
      dnf(ix, 2*fi + 1) = (T(6) * df3 * d5f2 + f3 * d6f2) * element_length;
    }
  }
} break;
case (7): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d6f2 = T(2) * d6flip(ix, fi) * flip(ix, fi) + T(12) * d5flip(ix, fi) * dflip(ix, fi) + T(30) * d4flip(ix, fi) * d2flip(ix, fi) + T(20) * d3flip(ix, fi) * d3flip(ix, fi);
      T d7f2 = T(2) * d7flip(ix, fi) * flip(ix, fi) + T(14) * d6flip(ix, fi) * dflip(ix, fi) + T(42) * d5flip(ix, fi) * d2flip(ix, fi) + T(70) * d4flip(ix, fi) * d3flip(ix, fi);
      dnf(ix, 2*fi)     = T(7) * df1 * d6f2 + f1 * d7f2;
      dnf(ix, 2*fi + 1) = (T(7) * df3 * d6f2 + f3 * d7f2) * element_length;
    }
  }
} break;
case (8): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d7f2 = T(2) * d7flip(ix, fi) * flip(ix, fi) + T(14) * d6flip(ix, fi) * dflip(ix, fi) + T(42) * d5flip(ix, fi) * d2flip(ix, fi) + T(70) * d4flip(ix, fi) * d3flip(ix, fi);
      T d8f2 = T(2) * d8flip(ix, fi) * flip(ix, fi) + T(16) * d7flip(ix, fi) * dflip(ix, fi) + T(56) * d6flip(ix, fi) * d2flip(ix, fi) + T(112) * d5flip(ix, fi) * d3flip(ix, fi) + T(70) * d4flip(ix, fi) * d4flip(ix, fi);
      dnf(ix, 2*fi)     = T(8) * df1 * d7f2 + f1 * d8f2;
      dnf(ix, 2*fi + 1) = (T(8) * df3 * d7f2 + f3 * d8f2) * element_length;
    }
  }
} break;
case (9): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d8f2 = T(2) * d8flip(ix, fi) * flip(ix, fi) + T(16) * d7flip(ix, fi) * dflip(ix, fi) + T(56) * d6flip(ix, fi) * d2flip(ix, fi) + T(112) * d5flip(ix, fi) * d3flip(ix, fi) + T(70) * d4flip(ix, fi) * d4flip(ix, fi);
      T d9f2 = T(2) * d9flip(ix, fi) * flip(ix, fi) + T(18) * d8flip(ix, fi) * dflip(ix, fi) + T(72) * d7flip(ix, fi) * d2flip(ix, fi) + T(168) * d6flip(ix, fi) * d3flip(ix, fi) + T(252) * d5flip(ix, fi) * d4flip(ix, fi);
      dnf(ix, 2*fi)     = T(9) * df1 * d8f2 + f1 * d9f2;
      dnf(ix, 2*fi + 1) = (T(9) * df3 * d8f2 + f3 * d9f2) * element_length;
    }
  }
} break;
case (10): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d9f2 = T(2) * d9flip(ix, fi) * flip(ix, fi) + T(18) * d8flip(ix, fi) * dflip(ix, fi) + T(72) * d7flip(ix, fi) * d2flip(ix, fi) + T(168) * d6flip(ix, fi) * d3flip(ix, fi) + T(252) * d5flip(ix, fi) * d4flip(ix, fi);
      T d10f2 = T(2) * d10flip(ix, fi) * flip(ix, fi) + T(20) * d9flip(ix, fi) * dflip(ix, fi) + T(90) * d8flip(ix, fi) * d2flip(ix, fi) + T(240) * d7flip(ix, fi) * d3flip(ix, fi) + T(420) * d6flip(ix, fi) * d4flip(ix, fi) + T(252) * d5flip(ix, fi) * d5flip(ix, fi);
      dnf(ix, 2*fi)     = T(10) * df1 * d9f2 + f1 * d10f2;
      dnf(ix, 2*fi + 1) = (T(10) * df3 * d9f2 + f3 * d10f2) * element_length;
    }
  }
} break;
case (11): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d10f2 = T(2) * d10flip(ix, fi) * flip(ix, fi) + T(20) * d9flip(ix, fi) * dflip(ix, fi) + T(90) * d8flip(ix, fi) * d2flip(ix, fi) + T(240) * d7flip(ix, fi) * d3flip(ix, fi) + T(420) * d6flip(ix, fi) * d4flip(ix, fi) + T(252) * d5flip(ix, fi) * d5flip(ix, fi);
      T d11f2 = T(2) * d11flip(ix, fi) * flip(ix, fi) + T(22) * d10flip(ix, fi) * dflip(ix, fi) + T(110) * d9flip(ix, fi) * d2flip(ix, fi) + T(330) * d8flip(ix, fi) * d3flip(ix, fi) + T(660) * d7flip(ix, fi) * d4flip(ix, fi) + T(924) * d6flip(ix, fi) * d5flip(ix, fi);
      dnf(ix, 2*fi)     = T(11) * df1 * d10f2 + f1 * d11f2;
      dnf(ix, 2*fi + 1) = (T(11) * df3 * d10f2 + f3 * d11f2) * element_length;
    }
  }
} break;
case (12): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d11f2 = T(2) * d11flip(ix, fi) * flip(ix, fi) + T(22) * d10flip(ix, fi) * dflip(ix, fi) + T(110) * d9flip(ix, fi) * d2flip(ix, fi) + T(330) * d8flip(ix, fi) * d3flip(ix, fi) + T(660) * d7flip(ix, fi) * d4flip(ix, fi) + T(924) * d6flip(ix, fi) * d5flip(ix, fi);
      T d12f2 = T(2) * d12flip(ix, fi) * flip(ix, fi) + T(24) * d11flip(ix, fi) * dflip(ix, fi) + T(132) * d10flip(ix, fi) * d2flip(ix, fi) + T(440) * d9flip(ix, fi) * d3flip(ix, fi) + T(990) * d8flip(ix, fi) * d4flip(ix, fi) + T(1584) * d7flip(ix, fi) * d5flip(ix, fi) + T(924) * d6flip(ix, fi) * d6flip(ix, fi);
      dnf(ix, 2*fi)     = T(12) * df1 * d11f2 + f1 * d12f2;
      dnf(ix, 2*fi + 1) = (T(12) * df3 * d11f2 + f3 * d12f2) * element_length;
    }
  }
} break;
case (13): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d12f2 = T(2) * d12flip(ix, fi) * flip(ix, fi) + T(24) * d11flip(ix, fi) * dflip(ix, fi) + T(132) * d10flip(ix, fi) * d2flip(ix, fi) + T(440) * d9flip(ix, fi) * d3flip(ix, fi) + T(990) * d8flip(ix, fi) * d4flip(ix, fi) + T(1584) * d7flip(ix, fi) * d5flip(ix, fi) + T(924) * d6flip(ix, fi) * d6flip(ix, fi);
      T d13f2 = T(2) * d13flip(ix, fi) * flip(ix, fi) + T(26) * d12flip(ix, fi) * dflip(ix, fi) + T(156) * d11flip(ix, fi) * d2flip(ix, fi) + T(572) * d10flip(ix, fi) * d3flip(ix, fi) + T(1430) * d9flip(ix, fi) * d4flip(ix, fi) + T(2574) * d8flip(ix, fi) * d5flip(ix, fi) + T(3432) * d7flip(ix, fi) * d6flip(ix, fi);
      dnf(ix, 2*fi)     = T(13) * df1 * d12f2 + f1 * d13f2;
      dnf(ix, 2*fi + 1) = (T(13) * df3 * d12f2 + f3 * d13f2) * element_length;
    }
  }
} break;
case (14): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d13f2 = T(2) * d13flip(ix, fi) * flip(ix, fi) + T(26) * d12flip(ix, fi) * dflip(ix, fi) + T(156) * d11flip(ix, fi) * d2flip(ix, fi) + T(572) * d10flip(ix, fi) * d3flip(ix, fi) + T(1430) * d9flip(ix, fi) * d4flip(ix, fi) + T(2574) * d8flip(ix, fi) * d5flip(ix, fi) + T(3432) * d7flip(ix, fi) * d6flip(ix, fi);
      T d14f2 = T(2) * d14flip(ix, fi) * flip(ix, fi) + T(28) * d13flip(ix, fi) * dflip(ix, fi) + T(182) * d12flip(ix, fi) * d2flip(ix, fi) + T(728) * d11flip(ix, fi) * d3flip(ix, fi) + T(2002) * d10flip(ix, fi) * d4flip(ix, fi) + T(4004) * d9flip(ix, fi) * d5flip(ix, fi) + T(6006) * d8flip(ix, fi) * d6flip(ix, fi) + T(3432) * d7flip(ix, fi) * d7flip(ix, fi);
      dnf(ix, 2*fi)     = T(14) * df1 * d13f2 + f1 * d14f2;
      dnf(ix, 2*fi + 1) = (T(14) * df3 * d13f2 + f3 * d14f2) * element_length;
    }
  }
} break;
case (15): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  arma::Mat<T> d15flip;
  eval_lip_prim_dnf<T>(x, x0, d15flip, 15);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d14f2 = T(2) * d14flip(ix, fi) * flip(ix, fi) + T(28) * d13flip(ix, fi) * dflip(ix, fi) + T(182) * d12flip(ix, fi) * d2flip(ix, fi) + T(728) * d11flip(ix, fi) * d3flip(ix, fi) + T(2002) * d10flip(ix, fi) * d4flip(ix, fi) + T(4004) * d9flip(ix, fi) * d5flip(ix, fi) + T(6006) * d8flip(ix, fi) * d6flip(ix, fi) + T(3432) * d7flip(ix, fi) * d7flip(ix, fi);
      T d15f2 = T(2) * d15flip(ix, fi) * flip(ix, fi) + T(30) * d14flip(ix, fi) * dflip(ix, fi) + T(210) * d13flip(ix, fi) * d2flip(ix, fi) + T(910) * d12flip(ix, fi) * d3flip(ix, fi) + T(2730) * d11flip(ix, fi) * d4flip(ix, fi) + T(6006) * d10flip(ix, fi) * d5flip(ix, fi) + T(10010) * d9flip(ix, fi) * d6flip(ix, fi) + T(12870) * d8flip(ix, fi) * d7flip(ix, fi);
      dnf(ix, 2*fi)     = T(15) * df1 * d14f2 + f1 * d15f2;
      dnf(ix, 2*fi + 1) = (T(15) * df3 * d14f2 + f3 * d15f2) * element_length;
    }
  }
} break;
case (16): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  arma::Mat<T> d15flip;
  eval_lip_prim_dnf<T>(x, x0, d15flip, 15);
  arma::Mat<T> d16flip;
  eval_lip_prim_dnf<T>(x, x0, d16flip, 16);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d15f2 = T(2) * d15flip(ix, fi) * flip(ix, fi) + T(30) * d14flip(ix, fi) * dflip(ix, fi) + T(210) * d13flip(ix, fi) * d2flip(ix, fi) + T(910) * d12flip(ix, fi) * d3flip(ix, fi) + T(2730) * d11flip(ix, fi) * d4flip(ix, fi) + T(6006) * d10flip(ix, fi) * d5flip(ix, fi) + T(10010) * d9flip(ix, fi) * d6flip(ix, fi) + T(12870) * d8flip(ix, fi) * d7flip(ix, fi);
      T d16f2 = T(2) * d16flip(ix, fi) * flip(ix, fi) + T(32) * d15flip(ix, fi) * dflip(ix, fi) + T(240) * d14flip(ix, fi) * d2flip(ix, fi) + T(1120) * d13flip(ix, fi) * d3flip(ix, fi) + T(3640) * d12flip(ix, fi) * d4flip(ix, fi) + T(8736) * d11flip(ix, fi) * d5flip(ix, fi) + T(16016) * d10flip(ix, fi) * d6flip(ix, fi) + T(22880) * d9flip(ix, fi) * d7flip(ix, fi) + T(12870) * d8flip(ix, fi) * d8flip(ix, fi);
      dnf(ix, 2*fi)     = T(16) * df1 * d15f2 + f1 * d16f2;
      dnf(ix, 2*fi + 1) = (T(16) * df3 * d15f2 + f3 * d16f2) * element_length;
    }
  }
} break;
case (17): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  arma::Mat<T> d15flip;
  eval_lip_prim_dnf<T>(x, x0, d15flip, 15);
  arma::Mat<T> d16flip;
  eval_lip_prim_dnf<T>(x, x0, d16flip, 16);
  arma::Mat<T> d17flip;
  eval_lip_prim_dnf<T>(x, x0, d17flip, 17);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d16f2 = T(2) * d16flip(ix, fi) * flip(ix, fi) + T(32) * d15flip(ix, fi) * dflip(ix, fi) + T(240) * d14flip(ix, fi) * d2flip(ix, fi) + T(1120) * d13flip(ix, fi) * d3flip(ix, fi) + T(3640) * d12flip(ix, fi) * d4flip(ix, fi) + T(8736) * d11flip(ix, fi) * d5flip(ix, fi) + T(16016) * d10flip(ix, fi) * d6flip(ix, fi) + T(22880) * d9flip(ix, fi) * d7flip(ix, fi) + T(12870) * d8flip(ix, fi) * d8flip(ix, fi);
      T d17f2 = T(2) * d17flip(ix, fi) * flip(ix, fi) + T(34) * d16flip(ix, fi) * dflip(ix, fi) + T(272) * d15flip(ix, fi) * d2flip(ix, fi) + T(1360) * d14flip(ix, fi) * d3flip(ix, fi) + T(4760) * d13flip(ix, fi) * d4flip(ix, fi) + T(12376) * d12flip(ix, fi) * d5flip(ix, fi) + T(24752) * d11flip(ix, fi) * d6flip(ix, fi) + T(38896) * d10flip(ix, fi) * d7flip(ix, fi) + T(48620) * d9flip(ix, fi) * d8flip(ix, fi);
      dnf(ix, 2*fi)     = T(17) * df1 * d16f2 + f1 * d17f2;
      dnf(ix, 2*fi + 1) = (T(17) * df3 * d16f2 + f3 * d17f2) * element_length;
    }
  }
} break;
case (18): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  arma::Mat<T> d15flip;
  eval_lip_prim_dnf<T>(x, x0, d15flip, 15);
  arma::Mat<T> d16flip;
  eval_lip_prim_dnf<T>(x, x0, d16flip, 16);
  arma::Mat<T> d17flip;
  eval_lip_prim_dnf<T>(x, x0, d17flip, 17);
  arma::Mat<T> d18flip;
  eval_lip_prim_dnf<T>(x, x0, d18flip, 18);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d17f2 = T(2) * d17flip(ix, fi) * flip(ix, fi) + T(34) * d16flip(ix, fi) * dflip(ix, fi) + T(272) * d15flip(ix, fi) * d2flip(ix, fi) + T(1360) * d14flip(ix, fi) * d3flip(ix, fi) + T(4760) * d13flip(ix, fi) * d4flip(ix, fi) + T(12376) * d12flip(ix, fi) * d5flip(ix, fi) + T(24752) * d11flip(ix, fi) * d6flip(ix, fi) + T(38896) * d10flip(ix, fi) * d7flip(ix, fi) + T(48620) * d9flip(ix, fi) * d8flip(ix, fi);
      T d18f2 = T(2) * d18flip(ix, fi) * flip(ix, fi) + T(36) * d17flip(ix, fi) * dflip(ix, fi) + T(306) * d16flip(ix, fi) * d2flip(ix, fi) + T(1632) * d15flip(ix, fi) * d3flip(ix, fi) + T(6120) * d14flip(ix, fi) * d4flip(ix, fi) + T(17136) * d13flip(ix, fi) * d5flip(ix, fi) + T(37128) * d12flip(ix, fi) * d6flip(ix, fi) + T(63648) * d11flip(ix, fi) * d7flip(ix, fi) + T(87516) * d10flip(ix, fi) * d8flip(ix, fi) + T(48620) * d9flip(ix, fi) * d9flip(ix, fi);
      dnf(ix, 2*fi)     = T(18) * df1 * d17f2 + f1 * d18f2;
      dnf(ix, 2*fi + 1) = (T(18) * df3 * d17f2 + f3 * d18f2) * element_length;
    }
  }
} break;
case (19): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  arma::Mat<T> d15flip;
  eval_lip_prim_dnf<T>(x, x0, d15flip, 15);
  arma::Mat<T> d16flip;
  eval_lip_prim_dnf<T>(x, x0, d16flip, 16);
  arma::Mat<T> d17flip;
  eval_lip_prim_dnf<T>(x, x0, d17flip, 17);
  arma::Mat<T> d18flip;
  eval_lip_prim_dnf<T>(x, x0, d18flip, 18);
  arma::Mat<T> d19flip;
  eval_lip_prim_dnf<T>(x, x0, d19flip, 19);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d18f2 = T(2) * d18flip(ix, fi) * flip(ix, fi) + T(36) * d17flip(ix, fi) * dflip(ix, fi) + T(306) * d16flip(ix, fi) * d2flip(ix, fi) + T(1632) * d15flip(ix, fi) * d3flip(ix, fi) + T(6120) * d14flip(ix, fi) * d4flip(ix, fi) + T(17136) * d13flip(ix, fi) * d5flip(ix, fi) + T(37128) * d12flip(ix, fi) * d6flip(ix, fi) + T(63648) * d11flip(ix, fi) * d7flip(ix, fi) + T(87516) * d10flip(ix, fi) * d8flip(ix, fi) + T(48620) * d9flip(ix, fi) * d9flip(ix, fi);
      T d19f2 = T(2) * d19flip(ix, fi) * flip(ix, fi) + T(38) * d18flip(ix, fi) * dflip(ix, fi) + T(342) * d17flip(ix, fi) * d2flip(ix, fi) + T(1938) * d16flip(ix, fi) * d3flip(ix, fi) + T(7752) * d15flip(ix, fi) * d4flip(ix, fi) + T(23256) * d14flip(ix, fi) * d5flip(ix, fi) + T(54264) * d13flip(ix, fi) * d6flip(ix, fi) + T(100776) * d12flip(ix, fi) * d7flip(ix, fi) + T(151164) * d11flip(ix, fi) * d8flip(ix, fi) + T(184756) * d10flip(ix, fi) * d9flip(ix, fi);
      dnf(ix, 2*fi)     = T(19) * df1 * d18f2 + f1 * d19f2;
      dnf(ix, 2*fi + 1) = (T(19) * df3 * d18f2 + f3 * d19f2) * element_length;
    }
  }
} break;
case (20): {
  dnf.zeros(x.n_elem, 2 * x0.n_elem);
  arma::Mat<T> flip;
  eval_lip_prim_dnf<T>(x, x0, flip, 0);
  arma::Mat<T> dflip;
  eval_lip_prim_dnf<T>(x, x0, dflip, 1);
  arma::Mat<T> d2flip;
  eval_lip_prim_dnf<T>(x, x0, d2flip, 2);
  arma::Mat<T> d3flip;
  eval_lip_prim_dnf<T>(x, x0, d3flip, 3);
  arma::Mat<T> d4flip;
  eval_lip_prim_dnf<T>(x, x0, d4flip, 4);
  arma::Mat<T> d5flip;
  eval_lip_prim_dnf<T>(x, x0, d5flip, 5);
  arma::Mat<T> d6flip;
  eval_lip_prim_dnf<T>(x, x0, d6flip, 6);
  arma::Mat<T> d7flip;
  eval_lip_prim_dnf<T>(x, x0, d7flip, 7);
  arma::Mat<T> d8flip;
  eval_lip_prim_dnf<T>(x, x0, d8flip, 8);
  arma::Mat<T> d9flip;
  eval_lip_prim_dnf<T>(x, x0, d9flip, 9);
  arma::Mat<T> d10flip;
  eval_lip_prim_dnf<T>(x, x0, d10flip, 10);
  arma::Mat<T> d11flip;
  eval_lip_prim_dnf<T>(x, x0, d11flip, 11);
  arma::Mat<T> d12flip;
  eval_lip_prim_dnf<T>(x, x0, d12flip, 12);
  arma::Mat<T> d13flip;
  eval_lip_prim_dnf<T>(x, x0, d13flip, 13);
  arma::Mat<T> d14flip;
  eval_lip_prim_dnf<T>(x, x0, d14flip, 14);
  arma::Mat<T> d15flip;
  eval_lip_prim_dnf<T>(x, x0, d15flip, 15);
  arma::Mat<T> d16flip;
  eval_lip_prim_dnf<T>(x, x0, d16flip, 16);
  arma::Mat<T> d17flip;
  eval_lip_prim_dnf<T>(x, x0, d17flip, 17);
  arma::Mat<T> d18flip;
  eval_lip_prim_dnf<T>(x, x0, d18flip, 18);
  arma::Mat<T> d19flip;
  eval_lip_prim_dnf<T>(x, x0, d19flip, 19);
  arma::Mat<T> d20flip;
  eval_lip_prim_dnf<T>(x, x0, d20flip, 20);
  for (size_t ix = 0; ix < x.n_elem; ix++) {
    for (size_t fi = 0; fi < x0.n_elem; fi++) {
      // First shape function:  [1 - 2(x-xi)*lipxi(fi)] * L_i(x)^2  =  f1 * f2
      // Second shape function: (x-xi) * L_i(x)^2                    =  f3 * f2
      T f1 = T(1) - T(2) * (x(ix) - x0(fi)) * lipxi(fi);
      T df1 = -T(2) * lipxi(fi);
      T f3 = x(ix) - x0(fi);
      T df3 = T(1);
      T d19f2 = T(2) * d19flip(ix, fi) * flip(ix, fi) + T(38) * d18flip(ix, fi) * dflip(ix, fi) + T(342) * d17flip(ix, fi) * d2flip(ix, fi) + T(1938) * d16flip(ix, fi) * d3flip(ix, fi) + T(7752) * d15flip(ix, fi) * d4flip(ix, fi) + T(23256) * d14flip(ix, fi) * d5flip(ix, fi) + T(54264) * d13flip(ix, fi) * d6flip(ix, fi) + T(100776) * d12flip(ix, fi) * d7flip(ix, fi) + T(151164) * d11flip(ix, fi) * d8flip(ix, fi) + T(184756) * d10flip(ix, fi) * d9flip(ix, fi);
      T d20f2 = T(2) * d20flip(ix, fi) * flip(ix, fi) + T(40) * d19flip(ix, fi) * dflip(ix, fi) + T(380) * d18flip(ix, fi) * d2flip(ix, fi) + T(2280) * d17flip(ix, fi) * d3flip(ix, fi) + T(9690) * d16flip(ix, fi) * d4flip(ix, fi) + T(31008) * d15flip(ix, fi) * d5flip(ix, fi) + T(77520) * d14flip(ix, fi) * d6flip(ix, fi) + T(155040) * d13flip(ix, fi) * d7flip(ix, fi) + T(251940) * d12flip(ix, fi) * d8flip(ix, fi) + T(335920) * d11flip(ix, fi) * d9flip(ix, fi) + T(184756) * d10flip(ix, fi) * d10flip(ix, fi);
      dnf(ix, 2*fi)     = T(20) * df1 * d19f2 + f1 * d20f2;
      dnf(ix, 2*fi + 1) = (T(20) * df3 * d19f2 + f3 * d20f2) * element_length;
    }
  }
} break;
default: {
  std::ostringstream oss;
  oss << n << "th derivatives not implemented!\n";
  throw std::logic_error(oss.str());
}
  }
}

} // namespace detail
} // namespace polynomial_basis
} // namespace lib1dfem
} // namespace helfem

#endif
